{
  "name": "felipe-side-scroller",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "test": "node tests/game-logic-smoke.mjs",
    "check": "node --check public/core.js && node --check public/entities.js && node --check public/gameplay.js && node --check public/render.js && node --check public/boot.js",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.133.0"
  }
}
